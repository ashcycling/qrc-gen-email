---
apiVersion: v1
kind: Namespace
metadata:
  name: qrc-gen-email
  labels:
    name: qrc-gen
---
apiVersion: v1
kind: Service
metadata:
  namespace: qrc-gen-email
  name: qrc-gen-front-service
  labels:
    app: qrc-gen-front-service
spec:
  type: ClusterIP
  ports:
    - name: http
      targetPort: 3000
      port: 3000
      protocol: TCP
  selector:
    app: qrc-gen-front
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    kubernetes.io/ingress.class: traefik
  labels:
    app: qrc-gen-front
  name: qrc-gen-front-ingress
  namespace: qrc-gen-email
spec:
  # ingressClassName: traefik # Use the name of your IngressClass here
  rules:
  - host: qrc-gen.46.224.32.20.nip.io
    http:
      paths:
      - backend:
          service:
            name: qrc-gen-front-service
            port:
              number: 3000
        path: /
        pathType: ImplementationSpecific
  tls:
  - hosts:
    - qrc-gen.46.224.32.20.nip.io # Change by your domain
    secretName: qrc-gen-front
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: qrc-gen-email
  name: qrc-gen-front
  labels:
    app: qrc-gen-front
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qrc-gen-front
  template:
    metadata:
      name: front
      labels:
        app: qrc-gen-front
    spec:
      containers:
        - name: qrc-gen-front
          image: ghcr.io/ashcycling/frontend:v0.0.1
          imagePullPolicy: Always
          env:
          - name: BACKEND_HOST
            value: "qrc-gen-back-service.qrc-gen.svc.cluster.local"
          - name: BACKEND_PORT
            value: "8080"
          resources:
            requests:
              memory: "15Mi"
              cpu: "25m"
            limits:
              memory: "50Mi"
              cpu: "500m"